# 自进化 24/7 优化系统 — 设计讨论交接文档

> 用途:这份文档记录了一场关于"如何为工业级广告排序+定价问题设计一个 24 小时持续自驱进化系统"的讨论。
> 它从 milo-backend 目录起头,但话题与 milo-backend 无关,需迁到独立目录继续。
> 读完这份即可无缝接续讨论。整理日期:2026-06-14。

---

## 0. 元目标:我们到底在设计什么

把"一问一答的执行器",变成一个**有持久状态、会自我改进的闭环系统**:
- 不断**发现问题 → 提方案 → 实验验证 → 得经验教训**,闭环。
- 能**非阻塞地向人提需求**(要信息时记下来继续干别的,不空转等人)。
- 目标:**24 小时持续运行**,无人值守也不跑歪、不空转、不胡说。

一条贯穿始终的核心判断:
> **闭环的循环体本身是最简单的部分。真正决定成败的是两件事:(1) 一个我无法自欺的"外部判定信号"(oracle);(2) 一份能在上下文被压缩后依然存活的持久状态。**
> 没有 (1),系统会"进化"进幻觉里(自己改测试让它通过);没有 (2),跑长了就忘了北极星。

---

## 1. 业务问题(具体场景)

- 一个复杂业务场景,**2 个页面**,DAU **几千万**。
- 算法可控的**展位 3~4 个**,每个展位做两件事:**排序 + 任务定价**(都已接算法)。
- 任务两类:
  - **活跃任务**:做点击/活跃。
  - **商业化任务**:做收入,分**广告**和**游戏**两类。
- **目标:在对 DAU 影响尽量小的前提下,尽量提升收入。**
- DAU 与收入有换算率,例:**一个任务点击值 0.1 元**。

### 数学化(决定整个系统的重心)

每个曝光机会、每个候选任务 i,给一个**统一货币口径的"混合价值"**:
```
blended_value(i) =
    expected_revenue(i)            若 i 是商业化任务(广告 eCPC×pClick / 游戏期望收入)
    α · pClick(i) · v_click        若 i 是活跃任务(v_click = 0.1 元/点击)
```
- 展位分配 = 按 blended_value 排序填坑;商业化任务再走拍卖定价。
- 全局目标(一天):**maximize Σ revenue,s.t. ΔDAU ≥ −预算**。
- **关键洞察:那个"0.1 元/点击"换算率,本质是这个约束优化的拉格朗日对偶变量 α,是"DAU 的价格"。**
  - α↑ → 活跃任务更易抢坑 → 点击/DAU↑、收入↓(保 DAU)
  - α↓ → 商业化占坑 → 收入↑、DAU 承压(收割收入)
- 所以系统中枢控制量是 α:**自动调 α,让 DAU 卡在允许的预算线上,在这条线上把收入榨到最大。**

---

## 2. 框架的两种切法(当前共识 = 三层)

### 2a. 最初的七步闭环(一次迭代的内部流程)
① 业务指标数据解读/诊断 → ② 机会识别+可证伪假设 → ③ 优先级 → ④ 干预/实验设计 → ⑤ 灰度执行 → ⑥ 带对照的统计判读 → ⑦ 学习固化 → 回 ①。
- 关键纪律:② 假设必须**先写证伪条件再动手**;④ 改动要小且隔离;⑥ 判定信号必须 agent 伪造不了。
- "数据解读"在闭环里出现**两次**:① 解读世界(哪儿不对)、⑥ 解读实验(改动有没有用)。同一种能力。
- **一个自进化优化系统,本质 80% 是个诊断师。**

### 2b. 用户重构的三层(当前主用模型,比七步更好用)

```
┌─ 第1层:业务目标追踪 + 归因(常驻,只看不动手)──────────────┐
│  北极星指标 + 分解体系,每日/每小时追涨跌 → "哪涨哪跌+为什么"      │
└──────────────────────────┬──────────────────────────────────┘
                           │ 指出:哪里有机会 / 哪里在漏(归因→杠杆=桥)
                           ▼
┌─ 第2层:策略/模型优化闭环(动手)─────────────────────────────┐
│  三条战线,共用"离线找策略 → 上线验证 → 回收数据 → 总结教训":     │
│   1) 工程链路优化   2) 分人群策略   3) 模型优化(autoresearch)    │
│   共同心脏:先用【离线指标】筛出好策略,再上【线上实验】确认       │
└──────────────────────────┬──────────────────────────────────┘
                           │ 经验教训
                           ▼
┌─ 第3层:知识沉淀(团队知识库)────────────────────────────────┐
│  每轮总结进知识库 → 反哺:让第1层归因更准、第2层假设更聪明          │
└─────────────────────────────────────────────────────────────┘
        (第3层回流到第1、2层,三层才闭环)
```

- **第1层追"结果"(我们不直接控),第2层调"杠杆"(我们能控)。归因是连接两层的桥。**
- **离线指标可信度是最大风险**:离线涨了线上不涨(off-policy bias)是头号翻车点。所以第1层的线上结果追踪要**兜底**,做第2层离线判断对不对的最终裁判。
- 第2层三条战线共用闭环,区别只在**杠杆**和**离线尺子**:
  - 1) 工程链路优化:杠杆=链路打分参数/阈值;离线尺子=回放线上打分日志看指标变化。(用户有经验)
  - 2) 分人群策略:杠杆=分人群参数;离线尺子=同样回放但按人群切片。(用户有经验)
  - 3) 模型优化:杠杆=模型本身(特征/结构/重训);离线尺子=离线模型指标(GAUC 等);**autoresearch** 把"提模型改动假设→迭代"自动化,最前沿最难。
- 用户的关键经验(与"oracle 是命门"殊途同归):**必须先有离线指标,让模型搜可调参数,看调完指标怎么变,离线评估筛出好策略,再上线验证。**

---

## 3. 第1层深入(已讨论到可落地)

第1层目标:**把"业务目标涨跌"自动翻译成"哪个因子/哪个维度导致 + 为什么 + 多确信"。**
三块拼成:**分解树(骨架)→ 自动归因(下钻)→ 双向验证(防归错)。**

### 3a. 分解树(一切归因挂在它上面)
```
【收入树】(乘法分解)
总收入
├─ 商业化收入
│   ├─ 广告收入 = 广告曝光 × 填充率 × CTR × CPC
│   └─ 游戏收入 = 游戏曝光 × 转化率 × 单次转化价值
└─ (活跃侧不直接产收入,但喂 DAU)

【杠杆】(夹在两棵树中间,唯一能动的)
排序:各任务类型在各展位的曝光分配 / 定价:商业化任务出价/eCPM / α:活跃vs商业化权衡

【DAU 树】(加法/留存分解)
DAU = 新增用户 + 回访用户
        └─ 回访 = Σ_cohort(cohort规模 × 留存率)←受活跃任务点击/体验影响
```
每个叶子再按维度切片:**页面(2)× 展位(3~4)× 人群 × 新老用户**。
> 树切得对(因子可定位、且每叶子对应一个杠杆),归因才能落到可行动粒度。**树是地基中的地基。**

### 3b. 自动归因(沿树下钻 + 定位维度 + 判内外因)— 走通的例子「收入 −3%」
1. 顶层下钻:商业化 −3%,活跃侧持平 → 进商业化。
2. 再下钻:游戏收入 −12%,广告持平 → 锁定游戏。
3. 拆因子:游戏收入=曝光×转化×价值;游戏曝光 −10%(填充率掉),其余持平 → 锁定"游戏填充率"。
4. 切维度:下降集中在 展位2 + 新用户。
5. 判内外因:变更日志显示我们没动展位2参数 → 偏外因;查外部=某热门游戏下线,游戏供给少 → 外因确认。
6. 输出:收入 −3%,根因=游戏库存外生减少→展位2新用户填不满→曝光少→收入少;内部杠杆无责。
- **桥**:对应杠杆="展位2 对游戏降门槛 / 广告回填空位" → 这句递给第2层。

### 3c. 双向验证(防伪相关,来自材料闭环 arXiv 2509.18604)
24/7 无人值守最大风险=归因归错(某小时两件事碰巧一起动)。四象限:
```
                  机制能讲通吗?(top-down:领域知识/LLM 说出因果链)
                  能                          不能
数据上显著相关?  ┌──────────────────┬──────────────────────┐
(bottom-up:统计) │ ✅ 确信归因        │ ⚠️ 疑似伪相关→标记/设小实验验 │
   是            │                  │                      │
                 ├──────────────────┼──────────────────────┤
   否            │ 🔍 机制待数据支持  │ ❌ 无关,忽略          │
                 └──────────────────┴──────────────────────┘
```
> 只有"数据相关 + 机制可解"两关都过才算确信归因。这是无人值守还不胡说的根本安全带。

### 3d. 24/7 跑法
- 每小时刷新分解树各节点值,跟基线比(同比上周同日消周内效应 / vs forecast)。
- **噪声地板**(借 CORAL 的 epsilon):每节点有波动带,只有超出才触发下钻归因,**别每小时对抖动喊狼**。
- 触发后分级:外因→知会记录;**内因异常(我们某变更搞坏了)→ 告警,可能回滚**(第1层兜底第2层)。

### 3e. 第1层产出 = 一份"诊断"(交给第2层的接口)
| 字段 | 例子 |
|---|---|
| 指标&偏离 | 总收入 −3%(超噪声地板) |
| 根因因子 | 游戏填充率↓(展位2×新用户) |
| 内外因 | 外因:游戏库存外生减少 |
| 置信度 | 确信(数据+机制双吻合) |
| 对应杠杆(桥) | 展位2对游戏降门槛 / 广告回填空位 |

---

## 4. 调研结论(论文 + 开源 + CORAL)

调研用 deep-research 跑过(112 agent、对抗式核实)。两个方向:

### 4a. 方向1 论文(已核实)— 范式 → 对应闭环
- **两篇 2025 综述**(arXiv 2507.21046 / 2508.07407):统一 taxonomy("进化什么/何时/如何/在何处")+ "System Inputs–Agent–Environment–Optimisers"反馈环。整体脚手架。
- **EvolveR**(2510.16079):在线交互→**离线自蒸馏出自然语言"策略原则"+结构化三元组**→存带使用/成功计数+剪枝的经验库→RL 更新。**直接命中第3层(经验固化),最可移植**(RL 改权重那段工业不照搬)。
- **材料闭环**(2509.18604):诊断=统计关联/根因研究;**top-down LLM 理论 + bottom-up 数据双向验证防 data fishing**。**直接服务第1层 + 防自欺**(就是 3c 那套)。
- **STOP**(2310.02304,微软,开源):递归自改进,**显式测量"绕过 sandbox / 博弈效用函数"频率**(实际未观察到成功绕过,作残余风险)。**防 reward hacking 的学术先例**。
- **Darwin Gödel Machine**(2505.22954,Sakana+UBC):**agent 归档(archive)开放进化防局部最优** + 经验性 benchmark 验证替代证明。SWE-bench 20%→50%。
- **BioDiscoveryAgent**(ICLR 2025,2405.17631):闭环实验设计;**独立 critic agent 给第二意见**;多工具;不训 ML 模型/不要 acquisition function,靠 LLM 知识选实验。
- **NovelSeek/InternAgent**(上海AI Lab,开源,2505.16938):完整闭环多 agent 自治科研;**非阻塞人类反馈**(自动评分高才介入)。可参考骨架,但缺 ①③⑥⑦。
- **AlphaEvolve / FunSearch / Promptbreeder / Gödel Agent**:LLM+进化式程序搜索,靠**自动评估器(evaluator)**迭代。印证"评估器质量=进化质量上限"。
- 会议:**ICML 2025 有 PRAL(Programmatic Representations for Agent Learning)workshop**(已核实)。其余顶会专门 track 未逐一核实。
- 被驳回的过强说法:"AlphaEvolve 发现可证明正确、跨域超 SOTA 的新算法"(2票驳回,不采纳)。
- **诚实缺口**:全是研究原型,**没有一个自带工业生产要素**(灰度/canary、带对照统计、off-policy/bandit、α 约束优化)。只能借鉴机制,不能搬代码。**真正难的"在线实验+约束优化底座"没人替我们解决。**
- **方向2(开源落地)这轮没做透**(核实预算给了方向1)。已捞到但未深评的相关仓库:**OBP/zr-obp**(off-policy 评估,对应⑥/预筛)、**Ax + BoTorch**(贝叶斯实验设计,对应③/④)、OpenEvolve(程序层)、GEPA/DSPy(prompt/程序层)、Trace/TextGrad(NL反馈优化)。**待补一轮。**

### 4b. CORAL 深读(MIT+NUS+Stanford+Meta Superintelligence Lab;开源 Apache-2.0;arXiv 2604.01658;~724 stars;活跃)
**定位:self-evolving agents 的基础设施,原生集成 Claude Code/Codex/Cursor 等。是调研里唯一"既是研究又能当工程脚手架"的。**

**数据模型(4 类型):**
- `Task`(优化目标)、`Attempt`(commit+agent+score+status+parent_hash 血缘;status∈pending/improved/baseline/regressed/reverted/crashed/timeout)、`Score`(value:float|None + name + explanation + metadata)、`ScoreBundle`(**scores:dict[str,Score] 多指标 + aggregated:float + is_public + feedback:str 自然语言 + compute_aggregated(weights)**)。
- 两个关键点:**ScoreBundle 天生多指标**(能装 revenue/DAU/guardrail + 聚合 + NL 读出);**status="pending" 是一等状态、Score.value 可 None**(原生容忍"提交了还没评出分"=异步实验的落脚点)。

**GRADER(整个系统唯一接口契约):** `async grade(codebase_path, tasks) -> ScoreBundle`。
- daemon:长驻进程 watch `.coral/public/attempts/`;每个 pending 在 **`git worktree add --detach <commit>` 隔离检出**里评(agent 继续提交不扰动);幂等;默认串行;超时强杀→timeout/grader_error。
- 评完跟**该 agent 自己历史最优**比 → improved/baseline/regressed。
- **budget_class:real(计 plateau)/ tune(廉价预筛,不计 plateau)/ grader_error**。← 现成的"贵真测 vs 廉价预筛"二分。

**HEARTBEAT(周期中断驱动的反射引擎):** 注册的 action,各有独立触发器:
- `interval`(每 N eval)→ **reflect**(本地:写实验 note 锚定分数变化+因果+信心+下个预测,蒸馏 skill)、**consolidate**(全局:同主题 3+ note 合成 synthesis/connections/open-questions)、lint_wiki。
- `plateau`(连续 N eval 无改进,**epsilon=噪声地板**,小抖动不重置 streak)→ **pivot**(不是收尾信号,是换方向:攻最难未试想法,区分"被证据否决vs被畏难否决",承诺≥3 eval,声明 lane/posture/budget/abandon-if)。
- **islands**:多子种群各自 attempts/eval_count,岛间迁移=开放进化防局部最优(FunSearch/DGM 同思路)。
- role 文件带 generation 计数 + History 留痕(drift visibility)。

**承重接缝(我们要对接的 5 个):** GraderInterface.grade()、ScoreBundle、budget_class real/tune、status vs own-best + eval_count 时钟、status="pending"。

**根本张力:** CORAL 的 daemon 是"看到 pending → timeout 内**同步**评出 → finalize",默认评测秒~分钟、便宜、可大量重跑。**我们的评测要几天(DAU 几周)。**

---

## 5. 调研对三层框架的净启发(尤其 24/7)

总判断:**调研不改三层"做什么",但 CORAL 几乎就是"怎么让它 24 小时不停跑"的现成答案。**

- **第1层(归因)** ← 材料闭环的"统计归因 + 理论/数据双向验证防伪相关" + 噪声地板防乱归因。(CORAL 最不覆盖这层——它只 grade 不诊断业务。)
- **第2层(优化)** ← evaluator/off-policy 驱动搜索(战线1/2)+ autoresearch 范式(战线3)+ pivot 防空转 + islands 持续探索 + budget_class real/tune = 离线预筛 vs 线上确认。
- **第3层(沉淀)** ← EvolveR 自然语言经验蒸馏 + CORAL 的 reflect/consolidate/skills/roles 知识库实现。

**24/7 = CORAL 的"操作系统"** — 持续运行需要五样,CORAL 全有:
| 持续运行需要 | CORAL 机制 |
|---|---|
| 不靠人催的时钟/驱动器 | heartbeat |
| 不追噪声乱动 | plateau + epsilon 噪声地板 |
| 卡住会换方向不空转 | pivot |
| 长跑不忘越跑越聪明 | consolidate + role 留痕 |
| 持续开放探索不困局部最优 | islands |
| 要人补信息但不卡住 | (NovelSeek)非阻塞人类反馈 |
> 持续运行 ≠ 一个 while 死跑,= heartbeat 驱动 + 噪声地板防乱动 + pivot 防空转 + islands 持续探索 + 知识库防遗忘 + 非阻塞问人 这一整套"长跑纪律"。

**我们特有、谁都没解决的点 = 慢。** "24 小时持续"对我们 = **三个时钟同时转**:
| 时钟 | 跑什么 | 节拍 | 持续发热? |
|---|---|---|---|
| 小时级 | 第1层追踪+归因 | 每小时 | 一直热 |
| 持续 | 第2层离线:回放/搜参/离线评估/autoresearch | 不停 | **24h 算力主战场** |
| 天~周级 | 第2层线上:A/B 灰度→回收→总结→进知识库 | 实验周期 | 并行等多个实验成熟 |
> 真正能 7×24 满负荷的是**第1层追踪归因 + 第2层离线搜索**(不花真流量)。线上实验是低频最终裁判。**这让"离线指标是命门"更重要:离线层是 24h 主战场,线上是低频裁判。**

---

## 6. ExperimentGrader 设计(已起草,较深,供后续细化)

把 CORAL 的 grade() 实现成"线上 A/B + α 约束 + 长滞后 DAU holdout"。六个决策:
1. **同步评 → launch+reconcile 两段**:grade() 只注册实验返回 pending(不评分、eval_count 不++);另起 reconciler 守护进程,实验读出成熟后回填 score 才 eval_count++。**eval 时钟与墙钟解耦**:一个 eval=一次成熟读出,plateau 自动变成"连续 N 个实验无显著提升"。
2. **ScoreBundle 装约束**:scores={revenue_delta, dau_delta, guardrail_adload...};aggregated=约束感知混合价值=revenue_delta + α·dau_delta,且任一 guardrail 破线直接判负(防 reward hacking)。
3. **improved 重定义**:从点比较→"约束下显著提升"(blended CI 下界>0 且 guardrail 未破 且 DAU 损失≤预算);inconclusive→grader_error(计 plateau streak 但不动 anchor)。
4. **epsilon=显著性阈值/MDE**;plateau=探索预算耗尽→pivot。
5. **补 CORAL 没有的"稀缺真流量配给层"**:tune=离线预筛(off-policy IPS/DR 或 BO 代理),real=只有预筛 EV 靠前的才升线上 A/B。
6. **DAU 长滞后=双时钟读出**:T+短(数天,revenue+短期DAU代理,可暂定 improved 小步放量);T+长(数周,读永久 holdout,回填同一 attempt 的 dau_delta;若长周期侵蚀超预算→把 improved 改判 **reverted** 并回滚)。α 是对偶控制器,用长周期 DAU 偏差调 α。
- 对账:除"同步 daemon→异步 launch+reconcile"是唯一大改,其余都是在 CORAL 既有契约里填工业语义。难点在 ExperimentGrader 内部:**约束感知评分(α+护栏)、稀缺实验配给、DAU 长滞后双时钟翻案**。
- ⚠️ 用户反馈此节过细、暂时跟不上;后续按需再展开,优先把第1/2层在用户颗粒度上走通。

---

## 7. 待用户拍板的设计点(非阻塞,先记)
1. **实验容量**:同时能跑多少互不干扰的 A/B?(决定配给层紧张度 + islands 开几个)
2. **α 现在是常量还是已有控制系统在调?**(决定对偶控制器是新建还是接管)
3. **DAU 有没有永久 holdout?**(决定长周期翻案能不能做;做不了只能退用代理指标,更危险)
4. 离线指标与线上结果的历史一致性如何?(off-policy bias 有多大,决定离线层可信度)

---

## 8. 下一步候选(讨论到此处中断)
- **A.(建议先做)** 第1层继续钻:**分解树具体怎么切**,细化到"每个叶子对应哪个杠杆"(为第2层铺路、夯地基)。
- **B.** 上第2层:拿到第1层诊断后,优化闭环怎么把"离线指标→搜策略→上线验证→回收"跑起来(用户最有经验、最关心)。
- 旁路待补:方向2 开源框架(OBP/Ax/BoTorch 等)补一轮聚焦调研,评估接入可行性。

---

## 9. 工作风格备注(用户偏好)
- 用户希望系统/讨论尽量"放手做完再报告,歧义按合理默认值走,把假设记下来结尾汇总,不中途逐点确认"(这是用户对"另一个项目"的期望;milo-backend 那个项目保持谨慎逐点确认)。
- 用户偏好**结构清晰、能跟得上的颗粒度**,反感过早陷入实现细节(如 ScoreBundle 字段级)。讲设计先给"结构 + 走通的例子",不进代码。
- 始终中文回复。

---

## 10. 嵌套 loop + 递归诊断 + 机会检测(2026-06-19 讨论新增)

本节是在第8节中断后续上的一轮讨论,主题:**第2层"搜策略"里最难的一环=模型/策略迭代,怎么让 AI 像资深算法工程师一样去诊断和优化。** 同时补了整体框架的一个重要重构(嵌套 loop)和"机会检测"这一新模式。

### 10.0 一个判断:让 AI 像资深算法工程师,不靠把它调聪明
- LLM 最不缺的就是"生成改进点子"(换结构/加特征/改 loss…),一口气列 30 个都挺专业。
- 真正区分资深 vs 新手的是三样 LLM 天生弱的东西:**① 诊断品味**(先做 error analysis 再提方案,80% 时间看坏 case)、**② 效应量先验/base rate**(知道 90% 聪明点子不动指标,真正动的常是"无聊的"——更新鲜数据/修特征穿越/纠偏)、**③ 约束感 + 敢重构问题**(知道 GAUC↑≠收入↑,拍卖吃校准不吃排序;最高杠杆常是改问题不是改模型)。
- 这三样弱的本质不是"推理能力",是 **grounding(扎根真实数据)+ 被现实纠偏**。补法 = 三件套:**会看数据(眼睛/仪器)+ 骗不了的离线尺子(oracle)+ 攒得下教训(案例库/base rate)**。即:把 AI 从"有想法的人"改造成"协议驱动 + 带仪器 + 有案例库 + 被 critic 怀疑"的诊断师。

### 10.1 资深诊断方法(模型层的"怎么看")
三条铁律:**定位 → 分层 → 证伪**(先收窄"哪里",再问"为什么",最后"怎么验")。
- 铁律1 定位先于理论:先把问题压到最小切片(页面×展位×人群×任务类型×时间),别在平均上想原因。
- 铁律2 沿"预测→结果"链分层,问题一定活在某一层:`特征/表征 → 打分(排序力) → 校准 → 排序/拍卖决策 → 线上结果`。
- 铁律3 标签和离线尺子都是嫌疑人:一半"模型问题"其实是标签问题(位置偏差/延迟转化/选择偏差)或尺子问题(离线涨线上不涨)。

场景特有的"诊断仪器"(看哪几张图),**第一件最被低估却最致命**:
| 仪器 | 看什么 | 为什么要命 |
|---|---|---|
| **分切片校准曲线**(预估 vs 实际 CTR) | 预估偏不偏 | 拍卖定价吃校准不吃排序;GAUC 满分也可能预估虚高→出价虚高→挤掉真高价值→收入掉 |
| 分切片 GAUC + 流量占比 | 排序力在哪垮、那块够不够大 | 找"排序退化+体量够大"的交集 |
| 线上/离线特征一致性 | 同请求两边特征是否一致 | 特征穿越是沉默杀手 |
| 打分分布漂移(PSI/KL) | 模型有没有悄悄腐烂 | 检测 model rot |
| 反事实回放(IPS/DR) | 新模型在日志上会怎样 | 上线前离线裁判,警惕 off-policy bias |
| 读最差坏 case | 模型在哪栽 | **资深直觉是这么长出来的,不是读论文** |

诊断阶梯(走通的例子「广告CTR在展位2×新用户掉5%」):0.是真的吗(噪声地板+管道完整性) → 1.定位(够大吗) → 2.**分层**(看切片GAUC没降→看校准曲线发现新用户预估偏高→锁定校准层→冷启特征缺失) → 3.内外因 → 4.重构问题+可证伪假设(只对新用户重标定,ECE应降GAUC不变,先跑最便宜的实验)。**价值全在第2步分层:新手跳去换模型,资深用校准曲线把问题从"模型层"重构到"校准/冷启层",杠杆和成本完全不同。**

### 10.2 怎么让 AI 学会这套诊断(四个机制,全落在三层框架里)
1. **诊断协议当强制器**:把"定位→分层→证伪"写成必须逐格走的 protocol(=CORAL 的 role/skill 文件),专治 AI"跳去解法"的毛病——走完校准/切片/标签/内外因才允许提改动。
2. **给眼睛(可调用仪器)**:协议每格挂一个能跑的工具(切片查询/画校准曲线/ablation/回放评估)。协议+仪器一体,把第1层归因做实。
3. **案例库注入先验(第3层)**:把过去"现象→根因→哪个仪器命中"沉淀成案例,用真实案例 few-shot,远胜通用知识。
4. **对抗式 critic**:专挑刺的 agent 固定问"相关当因果了?标签查了吗?切片够大吗?"——这是 3c 双向验证落到模型诊断上。

### 10.3 框架重构:嵌套 loop(整体一个大 evolve)
把第5节"三个时钟同时转"升华成**嵌套循环**(更准,因为点出了因果链:快 loop 喂慢 loop):
- **小快 loop(第1层,每小时)**:扫业务数据 → 归因 → 吐出"问题/优化点"(如"展位2×新用户 CTR −5%")。**不碰模型、不花流量,7×24 一直热。**
- **大慢 loop(第2层)**:接过问题 → 模型诊断协议(深归因)→ 假设 → 离线实验 → 线上A/B → 教训。慢、贵。
- **最外层 evolve**:第3层教训回流,让小快 loop 归因更准、大慢 loop 假设更聪明。

**关键对齐:诊断协议 = 自动归因的"更深一层递归",同一个 DNA**(定位→分解一层→落责到子节点→判内外因/哪一层→给可证伪结论)。区别只在递归到树的哪一层:
| | 第1层自动归因 | 模型诊断协议 |
|---|---|---|
| 对象 | 业务指标树,把杠杆/模型当黑盒 | 打开那个杠杆/模型的内部 |
| 定位到 | "展位2×新用户,CTR−5%,内因,杠杆X" | "校准层坏,根因冷启特征缺失" |
| 产出 | 诊断+桥(哪个杠杆负责) | 可验的修复假设 |
| 速度/成本 | 便宜、每小时、不碰流量 | 贵(烧算力/烧流量) |
第1层归因走到"桥"就停;诊断协议接着桥,把杠杆撬开继续往下归因。**同一个算法沿树递归,只是树换了(业务树→模型内部树)。**

### 10.4 两种扫描模式:异常检测 vs 机会检测
小快 loop 应同时跑两种**认知模式不同**的扫描:
- **异常检测=发现问题**:跟基线比,有东西掉了。本质"和过去的自己比,什么变坏了"。文档原第1层就是这个(噪声地板+同比+归因)。**只做这个=被动救火。**
- **机会检测=发现优化点**:什么都没掉但在漏钱。本质"和能达到的上限比,差在哪"。**要主动进化必须有它。**
两者尺子不同:异常要基线/forecast;机会要"潜力/前沿估计"。
> **用户当前优先级:先做增长,增长=收入增长(在 DAU 预算内把收入榨满,已确认)。→ 机会检测优先级更高。**

### 10.5 机会检测怎么做:到"可计算前沿"的 gap
本质=和一个**估计出的潜力前沿**比。**难点&危险全在前沿可不可信**——前沿估错=幻觉机会=系统追鬼(oracle 问题在这里更尖锐,比异常检测危险)。做法关键不是想花算法,是**挑那些能离线算、且不太骗你的前沿**。四个,按"杠杆×可信度"排序:

1. **约束松弛/影子价格(对偶)——首选**:α 是 DAU 约束的对偶变量。DAU 预算没花满(实际损失−0.3% < 预算−1.0%)=约束松弛=**α太高太保守**=机会:降α换收入。**它是这个优化问题的最优性条件本身**,最便宜(只看汇总)、最不骗人(基于真实实现非反事实)。**做收入增长第一个该盯它。**
2. **分配后悔(LP最优 vs 实际,在日志上算)**:用真实候选池+真实打分算"最优填坑本该拿多少",和实际比。后悔大→打分对但分配没拉满(工程链路,便宜可修);后悔小收入还低→打分本身不准(回模型诊断)。
3. **标杆切片前沿**:最好的可比切片当"能达到"的存在性证明,但**先扣合理差异(意图等 confounder),残差 gap 才是机会**。危险在可比性,必须配机制解释(3c)。
4. **反事实提升(OPE)**:用日志+IPS/DR(OBP/zr-obp)扫小扰动,挑高估计提升+方差可接受的。off-policy bias 已知,**只用来初筛排序,不直接信**。

**关键结构:机会检测复用同一棵分解树**,异常检测在每个节点问"变了什么",机会检测在同一批节点问"到潜力的 gap 多大"——树不重建、杠杆映射不重做,只把四个前沿当尺子挂上去。**异常/机会是同一套基础设施的两个模式开关。**

走通的例子(对偶松弛→增长):近7天约束松弛0.7% → 定位到"页面1×老用户"(全局α保了不需要保的人)→ 机制(老用户留存对点击不敏感,有数据)→ 量化可多换X收入 → 假设"分人群降α,收入↑且该群DAU不显著掉"(落在战线2分人群策略)→ 递大慢 loop 验。

机会排序打分:`机会分 ≈ 估计gap × 切片体量 × 前沿可信度 × 可控性 / 捕获成本`(前沿可信度直接体现:对偶高、OPE低)。
**防追鬼安全带**:每个机会必须带①依赖哪个前沿假设 ②置信度(数据+机制双过) ③最便宜的确认实验,才允许递大慢 loop;否则降级为"待核实信号"。

分工:重活是数值的(LP/OPE/切片残差/对偶),不是 LLM 推理。**数值工具**算前沿/gap/出候选;**LLM**决定该算哪个前沿、解释 gap 机制(扣 confounder)、判前沿可信度、排序+形成假设。

### 10.5.1 资深做机会检测的 taste(扫树时的判断,不在尺子上)
taste 不在四把尺子(机制谁都会挂),在三个判断时刻 + 一条统摄直觉。
- **统摄直觉(最值钱):四把尺子在量同一件事——边际有没有拉平。** 最优分配下,投下去的最后一单位(曝光/DAU预算)的边际混合价值**处处相等**;哪里不等就有钱,差多少=机会多大。对偶松弛=DAU约束边际没拉平到α;分配后悔=候选间边际没拉平;标杆切片=人群间边际没拉平;反事实=边际还有未吃提升。**所以资深不是挨个跑四把尺子,而是带一个问题扫树:"下一单位投这里和投别处,边际回报一样吗?"** 新手看平均(哪块收入低),资深看**边际**(哪块再多投一点回报最高却被压着)。机会永远在边际,不在平均。
- **扫之前·往哪指(slack 先验)**:不均匀扫。机会聚集在 ①异质人群套了全局策略的地方(全局α/全局排序套在响应天差地别的人群上,必错配——最浓机会味)②冷启/边界/低数据区 ③刚变过还没被重新优化的地方 ④高流量×高方差节点。先验靠第3层案例库注入。
- **扫之前·size-first triage**:还没诊断,先草稿纸估天花板。"全吃满也就0.05%收入,不值GPU-week"→当场毙。用潜力量级×可捕获性无情筛选,在任何工具跑之前。
- **读 gap·三个不孤立看**:①看边际不看平均(同上)②看结构不看点(50个点gap收敛成1个结构根因→1个高杠杆fix)③看约束耦合不孤立估(真机会是`边际收入/边际DAU成本`最高处,不是收入gap最大处)。
- **出 gap 后·"这钱为什么还在桌上?"反射(机会有罪推定)**:机会检测专属防自欺安全带。看到"免费的0.7%"先警惕不先兴奋——要么真没人看过(真机会),要么有隐藏约束让它其实不免费(幻觉)。讲不出"为什么没被捡走"的gap,八成是confounder/数据假象。配套:机制优先于统计、可控性立刻过滤。
- **装进系统**:①把协议第一性原理写成"扫边际梯度"而非"跑四把尺子" ②slack先验进案例库 ③"为什么还在桌上"做成critic必问项 ④size-first triage做成协议第一步。

### 10.6 旁路 deep-research 净启发(2026-06-19,23源/104声明/核实25→确认21驳回4)
**总判断:两条核心判断被文献强力印证,而我们最在乎的两块恰是全行业空白(好消息+警告:没现成代码可抄)。**
- 印证1「诊断驱动≠盲目调参」:最强系统都做结构化搜索,但**没一个做领域诊断**(切片/校准/标签/穿越)。
- 印证2「离线尺子是命门」:Shopee 论文实锤——离线代理指标对线上偏差**系统性且非对称,单个校准系数纠不正**,必须线上验。

对照三层框架:
| 我们的设计 | 文献状态 | 蓝本 |
|---|---|---|
| 诊断协议(10.2) | ✅有现成蓝本 | **DeepFix**(2026,开源Apache-2.0,arXiv:2603.14099) |
| 嵌套loop(10.3) | ✅工业已验证 | **Google/YouTube 自进化推荐**(arXiv:2602.10226) |
| 对抗critic(3c) | ✅有范式 | **Google AI co-scientist**(6 agent 锦标赛证伪) |
| 第3层教训回流 | ✅工业已验证 | YouTube **Experiment Journal**(记diff+离线/线上结果,"离线好线上差"负信号回灌) |
| **机会检测+对偶松弛(10.5)** | ⚠️**全行业空白** | 没人做;所有系统都在"修错"(=异常检测),无一做"找机会/到前沿gap" |
| **off-policy预筛(尺子4/budget_class tune)** | ⚠️几乎缺位 | agent论文几乎都直接靠线上A/B,反事实评估环节基本没有 |

最该抄的三个:
1. **DeepFix=诊断协议现成蓝本**:把ML调试做成cross-artifact诊断测试,先跑Deepchecks(最多80项)显式覆盖标签偏差/train-test穿越/弱切片定位/校准过拟合(正好是10.1那张仪器表),再三分析agent并行+一跨工件推理agent走"汇聚→假设→排序",根因grounding到知识库+联网+self-consistency。**=四件套的工程实现。** ⚠️只做tabular/NLP/CV,**无ranking专属检查**(GAUC/负采样偏差/position bias/延迟转化);只证协议覆盖面,未证实战有效。
2. **YouTube双环=嵌套loop工业背书**:离线高频提假设+在线低频验北极星,agent改优化器/结构/奖励(不止调参),真实显著收益。⚠️收益0.06%~0.14%量级、单厂自报、"新优化器"实为重发现RMSprop,别被"自主发现"措辞唬住。
3. **AI co-scientist锦标赛证伪**可直接做critic形态。

被核实驳回(别引用):❌Sortify"端到端自主闭环ranking agent"+GMV−3.6%→+9.2%(0-3,自报夸大,仅"离线-线上非对称偏差"框定为真);❌AIDE"3x奖牌/跨基准SOTA"(0-3);❌FML-bench"TheAIScientist>AIDE>Claude Code"排名(1-2,仅"探索多样性正相关性能"通过)。

其他参照点:MLE-bench(最佳o1+AIDE pass@1仅16.9%→pass@8 34.1%,**靠多次尝试非单次推理**;反思停在代码/文本层非数据中心诊断);EST(淘宝展示广告,人工设计非agent,+1.22%CTR/+3.27%RPM,作"离线改进→线上lift量级"参照);切片诊断现成工具(HiBug2/DebugAgent)仍只覆盖CV,迁移到ranking需改造。

**差异化白地(报告openQuestions明确列):** 把DeepFix式诊断协议+YouTube式自动迭代**端到端拼起来、且用机会检测(非异常检测)驱动**,目前无任何系统做过。代价:对偶松弛/分配后悔/机会前沿/off-policy预筛都得自己从运筹/OPE第一性原理搭,无前人兜底。

来源清单(核实过的主源):MLE-STAR arXiv:2506.15692;AIDE aide.ml + arXiv:2502.13138;UIUC survey 2508.02744;AI co-scientist 2502.18864;MLE-bench 2410.07095;DeepFix 2603.14099 + github.com/delcaux-labs/deepfix;YouTube 自进化 2602.10226;Shopee/Sortify 2603.27765;EST 2602.10811;FML-bench 2510.10472;HiBug2/DebugAgent 2501.16751。

### 10.7 机会检测的硬骨头:从单策略日志里估反事实边际(2026-06-19 深挖)
机会检测全 reduce 到"估边际",而边际=反事实=日志里没观测到的另一种策略。这是白地里最硬、无现成论文可抄的一块。

- **最 senior 的一刀:不估全局曲线,只估当前工作点的局部斜率。** 你要的只是"往哪挪、挪多少"走下一步。局部斜率(小扰动可得)比全局曲线(必须大范围外推=必然虚构)容易一个数量级。**铁律:不估超过下一步所需的曲线。**
- **估局部边际的信任阶梯**(最可信→最危险):
  | 层 | 方法 | 可信度 | 前提 |
  |---|---|---|---|
  | T1 | **注入 ε 探索**:小流量跑 α±δ 双边扰动 → 有限差分梯度 | 最高(真随机无偏) | 花一点真流量+常驻探索预算 |
  | T2 | 局部 off-policy(IPS/DR,OBP/zr-obp) | 小偏差可信、大外推不可信 | **必须记录 propensity** |
  | T3 | 自然变异+阈值断点(RD,拍卖 cutoff 附近近随机) | 局部干净 | 要有现成变异 |
  | T4 | 响应面模型(GP/dose-response) | 最危险,只在数据稠密区可信 | 外推=虚构,等于又造个可能不可信的尺子 |
  实战主体=T1(常驻极小 ε 探索预算→每节点持续拿无偏局部梯度,接 budget_class:real 的微剂量版);T2 是没法扰动时的退路,前提 propensity 当时记了。
- **本问题特有三皱褶(DeepFix/通用文献都不管=白地):**
  1. **两边际经约束耦合,要的是比值**:不单看 dRev/dα,要同时 dRev/dα 和 dDAU/dα,机会信号=`dRev/dDAU`=这一刀实际换到的 DAU 价格;> 当前α→还能降α榨收入(松弛),< 当前α→已透支。对偶松弛检测本质就是估这个比值。
  2. **position bias 污染边际曝光价值**:多给一个曝光拿到的点击被展示位置混淆,不去偏=把位置红利算成任务价值。这是 ranking 专属去偏,正是 DeepFix 不覆盖、需自建的检查项。
  3. **延迟反馈/双时钟(最麻烦)**:dRev/dα 快(几天)、dDAU/dα 慢(留存几周),两边际活在不同时钟=第6节双时钟在此精确重现。后果:扰动α后收入边际几天知道,DAU 边际要长滞后 holdout 才靠谱→在确认前 α 只能小步保守动(不是保守,是 DAU 边际本身慢)。
- **走通例子**:DAU 松弛0.7%触发→定位"页面1×老用户"→T1 在1%流量跑α±δ→dRev/dα几天出、dDAU/dα先短期代理估+挂长滞后holdout→算 dRev/dDAU > 当前α且机制讲通(老用户留存对点击不敏感,3c双过)→小步降该群分人群α(战线2),设 abandon-if(几周后holdout若DAU侵蚀超预算则回滚)。
- **纪律**:每个估出的边际挂"假设标签+置信度",critic 必问:propensity记了吗/扰动真随机吗/在数据支撑区内吗/position去偏了吗/DAU是代理还是长滞后holdout?讲不清的降级为"待核实信号",不准进决策。
- **分工**:数值工具跑微扰/有限差分/IPS-DR/RD/去偏;LLM 判"当前数据状况下哪种估法被许可"(如"这α区间无数据支撑,T4外推是虚构,改要T1微扰")、读边际、扣假设、形成移动。
- **三个硬 infra 前提(决定机会检测能不能做、能多激进):① 记录 propensity(每次决策用的概率/分数);② 永久 holdout(读长滞后 DAU 边际的唯一可靠途径,呼应第7节待拍点3);③ 常驻 ε 探索流量(持续供给无偏局部梯度)。** → **待用户在真实环境确认这三样现状。**

### 10.8 待续
- ⏭️ **晚点换线上真实环境继续探讨**(用户 2026-06-19 提出)。届时先确认上面三个 infra 前提(propensity 日志 / 永久 holdout / 常驻探索流量)的现状,再定机会检测的可行版本(有→T1,没有→退 T3/T4 弱版)。
- 进行中:第1层**归因小 loop 具体怎么做**(每小时、只看不动手、产出诊断喂第2层)。
- 待办:DeepFix 的 ranking 专属检查项清单(GAUC/负采样/position bias/延迟转化);off-policy 预筛(IPS/DR)接入。
